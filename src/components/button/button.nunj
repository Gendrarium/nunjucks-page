{% macro button(props) %}
	<button
		type="button"
		class="button{% if props.white %} button_white {%- endif %}{% if props.className %} {{ props.className }}{%- endif %}"
	>
		{{ props.children }}
	</button>
{% endmacro %}
